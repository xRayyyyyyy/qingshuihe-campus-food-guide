# 校园美食推荐助手 - 运行与测试指南

## 一、环境要求

- Python 3.8+（建议3.10-3.12，避免使用3.13）
- Node.js 14+
- npm 6+
- 稳定的网络连接

## 二、完整部署步骤

### 步骤1：创建Python虚拟环境

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate  # macOS/Linux
# 或 .venv\Scripts\activate  # Windows
```

### 步骤2：安装Python依赖

**方法A：正常安装**
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**方法B：遇到SSL问题时**
```bash
pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org \
    fastapi uvicorn python-dotenv httpx pydantic
```

**方法C：使用国内镜像**
```bash
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
    fastapi uvicorn python-dotenv httpx pydantic
```

### 步骤3：配置后端环境变量

复制 `.env.example` 为 `.env`：
```bash
cp .env.example .env
```

编辑 `.env` 文件：
```env
# 可选：高德地图Web服务Key（不填则使用本地数据）
AMAP_WEB_KEY=

# 可选：DeepSeek配置
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
```

**注意**：如果不配置高德Key，应用将使用本地20条模拟数据。

### 步骤4：验证后端代码

```bash
python3 -m compileall -q app
echo "代码语法检查通过"
```

### 步骤5：启动后端服务

```bash
.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
```

看到以下输出表示启动成功：
```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000
```

测试后端：
```bash
# 新终端
curl http://127.0.0.1:8000/api/health
# 应返回：{"status":"ok","message":"校园美食推荐助手运行正常"}
```

### 步骤6：安装前端依赖

```bash
cd frontend
npm install
```

### 步骤7：配置前端环境变量

编辑 `frontend/.env.local`：
```env
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_AMAP_JS_KEY=your_amap_js_web_key_here
```

编辑 `frontend/index.html`，替换第7行的KEY：
```html
<script type="text/javascript" src="https://webapi.amap.com/maps?v=2.0&key=替换为你的高德JS_KEY"></script>
```

**注意**：高德JS Key和Web服务Key是两个不同的Key，需要分别申请。

### 步骤8：启动前端服务

```bash
npm run dev -- --host 127.0.0.1 --port 5173
```

看到以下输出：
```
VITE v4.x.x  ready in xxx ms
➜  Local:   http://127.0.0.1:5173/
```

### 步骤9：访问应用

浏览器打开：http://127.0.0.1:5173

## 三、功能测试用例

### 测试用例1：正常推荐流程

**操作步骤**：
1. 打开应用，不做任何筛选
2. 点击"获取推荐"按钮

**预期结果**：
- 显示10条推荐结果
- 地图上显示对应标记
- 顶部显示"本地模拟数据（更新时间：2026-09-13）"或"高德实时POI数据"

### 测试用例2：区域筛选

**操作步骤**：
1. 选择校区区域："南门"
2. 点击"获取推荐"

**预期结果**：
- 显示南门区域的餐厅（秦云老太婆摊摊面、书亦烧仙草、瑞幸咖啡、南门川菜馆）
- 地图自动缩放到南门区域

### 测试用例3：预算筛选

**操作步骤**：
1. 设置预算上限：15元
2. 点击"获取推荐"

**预期结果**：
- 只显示人均价格≤15元的餐厅
- 价格为"暂无"的餐厅也会显示（高德实时POI可能无价格）

### 测试用例4：口味筛选

**操作步骤**：
1. 勾选口味："麻辣"、"重口味"
2. 选择区域："学校食堂"
3. 点击"获取推荐"

**预期结果**：
- 显示3食堂川菜窗口、风华餐厅烧烤窗口、风华麻辣烫等
- 对实时POI此筛选可能无效（因高德无口味标签）

### 测试用例5：距离和评分筛选

**操作步骤**：
1. 设置最远距离：500米
2. 选择最低评分：4.5分以上
3. 点击"获取推荐"

**预期结果**：
- 只显示距离≤500米且评分≥4.5的餐厅
- 如银桦餐厅（200米，4.5分）

### 测试用例6：用餐时段筛选

**操作步骤**：
1. 选择用餐时段："早餐"
2. 点击"获取推荐"

**预期结果**：
- 显示银桦粥铺、银桦早餐包子铺等支持早餐的店铺

### 测试用例7：忌口筛选

**操作步骤**：
1. 勾选忌口："花椒"、"小麦"
2. 点击"获取推荐"

**预期结果**：
- 不显示含花椒或小麦过敏原的餐厅
- 排除3食堂川菜窗口、秦云老太婆摊摊面等

### 测试用例8：关键词搜索

**操作步骤**：
1. 输入关键词："咖啡"
2. 点击"获取推荐"

**预期结果**：
- 显示瑞幸咖啡
- 同时搜索店名和菜系字段

### 测试用例9：组合筛选

**操作步骤**：
1. 选择区域："学校食堂"
2. 选择用餐时段："晚餐"
3. 设置预算上限：20元
4. 最低评分：4.0分以上
5. 点击"获取推荐"

**预期结果**：
- 返回同时满足所有条件的餐厅
- 可能显示银桦餐厅、2食堂、风华麻辣烫等

### 测试用例10：无结果处理

**操作步骤**：
1. 设置预算上限：5元（极低）
2. 点击"获取推荐"

**预期结果**：
- 显示"😔 没有找到符合条件的餐厅"
- 提供放宽条件的建议
- 不报错、不崩溃

### 测试用例11：详情查看

**操作步骤**：
1. 获取推荐后，点击任意餐厅卡片

**预期结果**：
- 弹出详情抽屉
- 显示完整信息（电话、地址、坐标、数据来源）
- 地图自动定位到该餐厅
- 点击背景或关闭按钮可关闭抽屉

### 测试用例12：地图交互

**操作步骤**：
1. 获取推荐后，点击地图上的标记

**预期结果**：
- 显示信息窗，包含餐厅基本信息
- 可以拖动地图、缩放
- 地图工具栏可用

### 测试用例13：重置功能

**操作步骤**：
1. 设置多个筛选条件
2. 点击"重置"按钮

**预期结果**：
- 所有筛选条件清空
- 自动获取全部推荐

### 测试用例14：空输入处理

**操作步骤**：
1. 关键词输入框输入空格
2. 点击"获取推荐"

**预期结果**：
- 正常返回结果，空格被忽略
- 不报错

### 测试用例15：后端异常处理

**操作步骤**：
1. 停止后端服务
2. 在前端点击"获取推荐"

**预期结果**：
- 显示错误提示："加载失败: 请求失败: xxx。请检查后端服务是否启动。"
- 不崩溃，可以继续操作

## 四、API测试

### 健康检查
```bash
curl http://127.0.0.1:8000/api/health
```

### 获取餐饮列表
```bash
curl 'http://127.0.0.1:8000/api/foods?campus_area=南门'
```

### 推荐接口
```bash
curl 'http://127.0.0.1:8000/api/recommend?campus_area=学校食堂&max_price=20&min_rating=4.0'
```

### 查看API文档
浏览器访问：http://127.0.0.1:8000/docs

## 五、常见问题

### Q1: 依赖安装失败（SSL错误）
**解决**：使用 `--trusted-host` 或国内镜像，见步骤2方法B/C

### Q2: 前端地图不显示
**解决**：检查 `index.html` 中的高德JS Key是否正确填写

### Q3: 推荐结果一直显示本地数据
**解决**：检查 `backend/.env` 中的 `AMAP_WEB_KEY` 是否正确配置

### Q4: Python 3.13兼容性问题
**解决**：建议使用Python 3.10-3.12，或更新 `requirements.txt` 中的pydantic版本

### Q5: 端口被占用
**解决**：修改启动命令中的端口号，同时更新前端 `.env.local` 中的API地址

## 六、性能优化建议

1. **生产环境部署**：使用 `npm run build` 构建前端静态文件
2. **API缓存**：对高德API结果增加缓存机制
3. **分页加载**：实现餐厅列表的分页或虚拟滚动
4. **图片优化**：为餐厅添加图片并进行懒加载
5. **数据库**：使用PostgreSQL替代JSON文件存储

## 七、扩展功能建议

1. 用户账号系统（收藏、评价）
2. 实时导航（调起高德地图APP）
3. 排队信息接入
4. 菜品详情和图片展示
5. 社交分享功能
6. 优惠券和活动推送
