# 高德地图API配置问题说明

## 问题诊断

### 错误信息
```
状态: 0
信息: USERKEY_PLAT_NOMATCH
```

### 问题原因
**USERKEY_PLAT_NOMATCH** 表示API Key的平台类型与使用方式不匹配。

高德地图有两种不同的Key：
1. **JS API Key** - 用于前端网页地图展示
2. **Web服务API Key** - 用于后端服务器端API调用

当前使用的 Key `YOUR_AMAP_JS_KEY` 可能是 JS API Key，无法用于后端 Web 服务 API。

## 解决方案

### 方案1：申请新的Web服务Key（推荐）

1. 访问高德开放平台：https://console.amap.com/dev/key/app
2. 点击"创建新应用"或选择现有应用
3. 点击"添加Key"
4. **重要**：Key类型选择"Web服务"（不是"Web端(JS API)"）
5. 填写Key名称（如：校园美食推荐助手-后端）
6. 提交后获得新的Web服务Key
7. 将新Key配置到 `backend/.env` 的 `AMAP_WEB_KEY`

### 方案2：仅使用本地数据（临时方案）

如果暂时无法申请Web服务Key，可以继续使用本地数据：
- 应用已经可以正常运行
- 使用20条本地模拟数据
- 所有功能都可以正常测试
- 顶部显示"本地模拟数据（更新时间：2026-09-13）"

## 关于坐标精准度问题

### 本地数据坐标
本地数据中的坐标是手工配置的区域锚点，并非每家店的精确坐标：

```python
"学校食堂": {
    "location": "103.9348,30.7606",  # 银桦餐厅附近（区域中心）
    "radius": 500
},
"南门": {
    "location": "103.9330,30.7565",  # 南门区域中心
    "radius": 300
},
"西门龙湖时代天街": {
    "location": "103.9280,30.7640",  # 龙湖时代天街区域中心
    "radius": 500
}
```

### 改善方案

#### 方案A：使用高德实时POI（推荐）
配置正确的Web服务Key后，高德会返回每家店的精确GPS坐标。

#### 方案B：优化本地数据坐标
如果继续使用本地数据，可以手动优化每家店的坐标：
