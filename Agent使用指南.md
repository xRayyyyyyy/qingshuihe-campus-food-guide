# 校园觅食 Agent 使用指南

第一版已实现自然语言条件提取、只读工具调用、会话内调整、比较与详情、条件调整建议，以及对未知数据的单独展示。

## 启动和 DeepSeek 配置

1. 编辑 `backend/.env`，填入自己的 `LLM_API_KEY`。已配置默认地址和模型：

   ```env
   LLM_BASE_URL=https://api.deepseek.com
   LLM_MODEL_ID=deepseek-chat
   LLM_API_KEY=在此填写你的密钥
   AMAP_WEB_KEY=
   ```

2. 双击 `stop.cmd`，再双击 `start.cmd`。后端在启动时读取配置。
3. 打开 <http://127.0.0.1:5173/>。配置成功后，对话区会显示服务已配置；是否能成功调用还取决于密钥、服务可用性和额度。

密钥仅保存在后端，不应填写到任何 `VITE_*` 变量。未配置模型时页面明确提示，对话不产生模拟的模型回答，表单可照常筛选。未配置高德时使用本地 20 条模拟餐饮数据。

## 可以尝试的对话

- “晚饭在食堂吃清淡点，人均15元以内。”
- 接着说：“换一家，预算可以到20元。”
- “为什么推荐这家？”
- “比较第一家和第二家。”（当前列表至少两家时）
- “取消预算限制。”
- “不要火锅，按价格从低到高推荐。”

页面会同步显示当前条件，并支持直接修改筛选。新的会话使用独立条件和候选；会话在一小时不活跃后过期，重启后端会清空会话。多人的预算请写明“每人多少元”；未知口味、过敏信息或实时营业状态不会被自动当作满足条件。

## 已实现的边界

- 使用一个异步 Agent，通过函数调用协议选择检索、详情、比较和条件调整工具；最多 3 轮工具循环、6 次业务工具调用、20 秒总期限。
- 当前调用使用轻量 `httpx` 适配层，借鉴 Hello-Agents 的工具、状态、上下文设计，没有安装存在章节版本差异的 HelloAgents 包。可通过替换模型适配层接入其他支持同一工具协议的运行时。
- 模型仅输出受验证的条件变化和候选 ID。价格、名称、评分等事实以及面向用户的具体解释由后端从查询快照生成。
- 原表单与对话使用同一套 `FoodService` 规则。预算、评分和排除条件都在服务器执行；“放宽条件”建议只有点击接受或明确表达后才生效。
- 同一会话按顺序处理，使用版本号拒绝旧状态更新；请求编号支持重试去重。不同会话的条件、历史和候选隔离。
- 未知价格、未核实完整的过敏原信息列入待确认；本地模拟数据的营业状态显示未知。地图需要前端高德 Key，距离仍是参考点距离。
- 路线、真实营业状态、菜单知识库、长期偏好持久化、多 Agent 规划尚未实现，需要对应数据源或后续需求。

## 接口和校验

- `GET /api/agent/status`：模型配置状态，不返回密钥。
- `POST /api/chat`：`message`、`session_id`、`request_id`、`expected_state_version` 和可选 `ui_changes`。
- 空 `message` 加 `ui_changes` 可执行表单查询，无需模型。
- 原 `GET /api/recommend` 和 `GET /api/foods` 继续提供兼容查询。
- API 文档：<http://127.0.0.1:8000/docs>。

在 `backend` 目录执行测试：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe evals\run_eval.py
```

离线测试使用固定数据和伪造模型响应检验业务契约、循环、异常及会话行为，不代表真实 DeepSeek 模型质量或线上响应时间。配置有效密钥后，应再用上述真实对话验收约束提取效果。

日志位于 `.runtime`。请求追踪仅记录追踪 ID、状态、调用数量和耗时，不记录密钥或用户完整对话。
