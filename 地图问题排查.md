# 地图不显示问题排查

## 可能的原因

1. **高德地图JS API加载失败** - 网络问题或Key配置问题
2. **Vue组件生命周期问题** - `mounted`时地图容器未准备好
3. **浏览器控制台错误** - JavaScript错误阻止地图初始化

## 排查步骤

请在浏览器中按F12打开开发者工具，检查：

### 1. Console标签
查看是否有错误信息：
- 高德地图加载错误
- JavaScript错误
- API Key错误

### 2. Network标签
查看是否成功加载：
- `https://webapi.amap.com/maps?v=2.0&key=...`
- 状态应该是200

### 3. Elements标签
检查地图容器：
```html
<div id="amap-container" class="amap-container"></div>
```
- 容器是否存在
- 高度是否为500px
- 是否有高德地图生成的子元素

## 临时解决方案

如果地图仍然不显示，可以尝试：

1. **硬刷新浏览器**：Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
2. **清除缓存**后重新加载
3. **检查高德Key配额**：可能已用完

## 请告诉我

打开 http://127.0.0.1:5173 后，在浏览器控制台（F12）看到什么错误信息？
