# 本地部署说明

项目位于 `F:\agent`。原始源码来自 [GitHub 项目](https://github.com/xRayyyyyyy/qingshuihe-campus-food-guide) `main` 分支的官方 ZIP；本目录没有 `.git` 提交历史。现已完成校园觅食 Agent 第一版改造，配置和对话示例见 [Agent 使用指南](Agent使用指南.md)。

## 启动与停止

双击 `start.cmd` 启动前后端。服务在后台运行，关闭启动窗口后仍可访问：

- 应用：<http://127.0.0.1:5173/>
- API 文档：<http://127.0.0.1:8000/docs>

双击 `stop.cmd` 停止本项目服务。重复启动会复用脚本已经启动的服务；端口被其他程序占用时提示冲突。停止脚本只处理记录中身份仍匹配的进程。

也可以在当前目录运行 PowerShell：

```powershell
.\start.ps1
.\stop.ps1
```

如果执行策略阻止脚本，请使用 `start.cmd` / `stop.cmd`，它们只为当前调用设置执行策略。运行记录和日志位于 `.runtime`，启动失败时查看其中的 `*.stderr.log`。

## DeepSeek 对话配置

编辑 `backend/.env`；若文件不存在，复制 `backend/.env.example`：

```env
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL_ID=deepseek-chat
LLM_API_KEY=
AMAP_WEB_KEY=
```

`LLM_API_KEY` 默认留空。填入自己的 DeepSeek API Key 后停止并重新启动服务。模型密钥仅保存在后端，不能填入前端 `VITE_*` 变量。配置状态只表示参数就绪，实际调用仍取决于密钥、额度和服务可用性。

当前没有模型密钥，对话区明确提示未配置，表单筛选仍可使用。Agent 不会生成模拟的模型回答。会话状态只保存在内存，后端重启后清空；一小时不活跃后会话过期。

## 高德与前端配置

后端 `AMAP_WEB_KEY` 留空时使用本地 20 条模拟餐饮数据。填写有效的高德 Web 服务 Key 后可查询 POI，失败区域独立回退到本地数据。

编辑 `frontend/.env.local`：

```env
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_AMAP_JS_KEY=
VITE_AMAP_SECURITY_CODE=
```

地图组件从 `VITE_AMAP_JS_KEY` 读取高德 JavaScript API Key，从 `VITE_AMAP_SECURITY_CODE` 读取对应安全密钥。按高德控制台配置这两个值后重新启动前端即可；无需修改 `frontend/index.html`。未配置时显示地图提示，列表、推荐、条件调整和详情仍可使用。

价格、评分、营业时段均按来源标明参考性质。没有实时营业证据时显示未知；距离不代表当前位置的路线距离。过敏原未核实完整的候选只进入待确认区域。

## 依赖与验证

需要 **Python 3.11+**（使用 `asyncio.timeout`）、Node.js 和 npm。依赖目录被删除时，在项目根目录执行：

```powershell
python -m venv backend\.venv
.\backend\.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
npm --prefix frontend install
```

在 `backend` 目录运行测试：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe evals\run_eval.py
```

在项目根目录构建前端：`npm --prefix frontend run build`。当前已通过 61 项自动测试、7 个离线业务场景和前端构建。浏览器中已验证表单推荐、无结果建议应用、过敏原待确认及详情流程。真实 DeepSeek 对话尚未验证，需有效密钥；离线评估不会调用模型或高德。

启停脚本绑定 `127.0.0.1`，当前部署用于本机访问。



## 区域圈选配置

启动后打开 http://127.0.0.1:8000/area-config 。工具由后端提供，自动读取前端地图 Key 和已有区域配置；直接打开根目录 HTML 也会跳转到此地址。

选择区域后拖动画圆或矩形，点击“保存区域”加入草稿，再点击“保存到后端”应用。配置保存在 `backend/app/services/area_config.json`，重启后保留；支持 JSON 预览与下载。矩形保留西南、东北坐标，后端按边界过滤高德结果。当前仅支持主应用已有的三个区域名称。查询仍受高德返回条数限制，不保证覆盖区域内所有商家；高德无结果时仍遵循原有本地模拟数据回退逻辑。


### 区域归属与边界（2026-09-17）

主页面切换区域会立即清除旧结果并自动查询。配置工具支持圆形、矩形和多边形，多边形逐点点击、双击结束，可沿校园边界排除临街商铺。重叠区域内的 POI 归属距离中心最近的区域（相等时按名称稳定排序），不再由查询顺序决定；任何区域边界修改都会改变查询缓存标识。高德回退模拟数据也需满足已配置区域归属，已删除区域不再触发线上查询或回退。无 Key 的纯离线演示仍使用原有模拟区域标签。

当前保存的圆形区域未自动改动。圈内不代表实际校内，区域边界须由使用者核实。高德地址与店名可能有歧义，归属按坐标和配置判断。
