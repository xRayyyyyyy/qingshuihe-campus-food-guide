# 当前项目状态

更新日期：2026-09-17。

P0 数据契约、P1 Agent 第一版与部分 P2 可靠性功能已经完成。使用 Vue 3、FastAPI 与异步 httpx，支持 DeepSeek 函数调用、高德 POI、确定性筛选、多轮会话及区域边界编辑。

## 已验证

- 68 项后端自动测试通过，7 个离线业务评估通过，前端生产构建通过。
- 本机已配置模型与高德 Web 服务 Key；真实 DeepSeek 接口连通、高德 POI 查询和地图显示通过。密钥不随源码发布。
- 浏览器验证首页、卡片详情、区域切换自动查询、圆形/矩形绘制与 JSON 预览。
- 多边形包含判断、配置持久化、重叠区域归属和回退边界有自动测试覆盖。
- 用户报告的苍洱云、老面包点归天街；紫燕百味鸡、袁妹饭店归南门，学校食堂查询不包含四家。

## 展示与启动

图文介绍见 README，静态前端展示页在 `docs/index.html`，截图在 `docs/images/`。
安装步骤见 README 与本地部署说明。启动后应用为 http://127.0.0.1:5173/ ，区域工具为 http://127.0.0.1:8000/area-config 。

## 仍需验证与后续能力

真实模型多轮任务质量、延迟与成本尚未形成完整评测报告。实时营业、实际步行路线、过敏原完整性没有核实保证。长期偏好持久化、菜单知识库与多 Agent 规划尚未实现。

会话保存在内存中；区域配置接口只面向本机使用，公网部署前需增加身份认证与访问控制。
