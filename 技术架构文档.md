# 校园美食推荐助手 - 技术架构文档

## 一、系统架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                         用户浏览器                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Vue 3 前端应用                            │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │  │
│  │  │筛选面板   │  │结果展示   │  │高德地图(JS API)   │   │  │
│  │  └──────────┘  └──────────┘  └──────────────────┘   │  │
│  └───────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTP/REST API
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    FastAPI 后端服务                          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                   路由层 (main.py)                     │  │
│  │    /api/health  /api/foods  /api/recommend           │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                 服务层 (services/)                     │  │
│  │  ┌──────────────────┐  ┌──────────────────────────┐  │  │
│  │  │ food_service.py  │  │  amap_service.py         │  │  │
│  │  │ • 推荐逻辑       │  │  • 周边搜索              │  │  │
│  │  │ • 筛选排序       │  │  • POI标准化             │  │  │
│  │  │ • 数据回退       │  │  • 区域配置              │  │  │
│  │  └──────────────────┘  └──────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                 数据层 (data/)                         │  │
│  │              foods.json (20条本地数据)                 │  │
│  └───────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTP API
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    高德地图Web服务                           │
│              /v3/place/around (周边搜索)                    │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

**前端**
- Vue 3.3.4 - 渐进式JavaScript框架
- Vite 4.4.9 - 下一代前端构建工具
- 原生JavaScript - 无额外UI框架依赖
- 高德地图JS API 2.0 - 地图展示

**后端**
- Python 3.8+ - 编程语言
- FastAPI 0.110+ - 现代Web框架
- Uvicorn - ASGI服务器
- httpx - 异步HTTP客户端
- pydantic - 数据验证
- python-dotenv - 环境变量管理

**外部服务**
- 高德地图Web服务API - POI搜索
- 高德地图JS API - 地图展示

## 二、数据模型

### 2.1 餐厅数据结构

```json
{
  "id": 1,                              // 唯一标识
  "name": "银桦餐厅",                    // 餐厅名称
  "campus_area": "学校食堂",             // 校区区域
  "cuisine": "中餐",                    // 菜系类型
  "taste": ["家常", "微辣"],            // 口味标签（数组）
  "avg_price": 15,                      // 人均价格（元）
  "rating": 4.5,                        // 评分（0-5）
  "distance": 200,                      // 距离（米）
  "opening_hours": "07:00-20:00",       // 营业时间
  "is_open": true,                      // 当前是否营业（布尔/null）
  "allergens": [],                      // 过敏原列表
  "meal_time": ["早餐", "午餐", "晚餐"], // 适合时段
  "latitude": 30.7606,                  // 纬度
  "longitude": 103.9348,                // 经度
  "address": "",                        // 详细地址（可选）
  "tel": "",                            // 联系电话（可选）
  "source": "本地备用数据"              // 数据来源标识
}
```

### 2.2 字段说明

| 字段 | 类型 | 必填 | 说明 | 来源 |
|------|------|------|------|------|
| id | int | 否 | 本地数据有，实时POI无 | 本地 |
| name | string | 是 | 餐厅或窗口名称 | 通用 |
| campus_area | string | 是 | 学校食堂/南门/西门龙湖时代天街 | 通用 |
| cuisine | string | 是 | 菜系或类型 | 通用 |
| taste | array | 否 | 口味标签，实时POI通常为空 | 本地 |
| avg_price | int/null | 否 | 人均价格，可能为null | 通用 |
| rating | float/null | 否 | 评分，可能为null | 通用 |
| distance | int | 是 | 距离区域中心的米数 | 通用 |
| opening_hours | string | 是 | 营业时间文本 | 通用 |
| is_open | bool/null | 否 | 实时营业状态，实时POI为null | 本地 |
| allergens | array | 否 | 过敏原，实时POI为空 | 本地 |
| meal_time | array | 否 | 适合时段，实时POI默认午晚餐 | 本地 |
| latitude | float | 是 | 纬度坐标 | 通用 |
| longitude | float | 是 | 经度坐标 | 通用 |
| address | string | 否 | 详细地址，实时POI有 | 实时 |
| tel | string | 否 | 联系电话，实时POI有 | 实时 |
| source | string | 是 | "本地备用数据"或"高德实时POI" | 通用 |

## 三、核心算法

### 3.1 推荐算法

**推荐流程**：
```python
def recommend_foods(filters):
    # 1. 获取候选集（优先实时POI）
    candidates = get_foods(campus_area)

    # 2. 多条件筛选
    candidates = filter_by_keywords(candidates, keywords)
    candidates = filter_by_taste(candidates, taste)
    candidates = filter_by_price(candidates, max_price)
    candidates = filter_by_distance(candidates, max_distance)
    candidates = filter_by_rating(candidates, min_rating)
    candidates = filter_by_meal_time(candidates, meal_time)
    candidates = filter_by_allergens(candidates, exclude_allergens)

    # 3. 排序
    candidates.sort(key=sort_key)

    # 4. 返回Top N
    return candidates[:limit]
```

**排序策略**：
```python
def sort_key(food):
    rating = food.get("rating") or 0        # 评分高优先
    distance = food.get("distance") or 9999 # 距离近优先
    has_price = 1 if food.get("avg_price") else 0  # 信息完整优先
    return (-rating, distance, -has_price)  # 多键排序
```

### 3.2 双数据源策略

```python
async def get_foods(campus_area):
    # 1. 尝试获取实时POI
    if has_amap_key:
        try:
            real_pois = await amap_service.search_nearby_pois(campus_area)
            if real_pois:
                return real_pois  # 成功则返回实时数据
        except Exception:
            pass  # 失败则继续

    # 2. 回退到本地数据
    local_foods = load_local_foods()
    if campus_area:
        local_foods = filter_by_area(local_foods, campus_area)

    # 3. 标记数据来源
    for food in local_foods:
        food["source"] = "本地备用数据"

    return local_foods
```

### 3.3 高德POI标准化

```python
def normalize_poi(poi, campus_area):
    # 解析经纬度
    location = poi["location"].split(",")
    longitude = float(location[0])
    latitude = float(location[1])

    # 解析扩展信息
    biz_ext = poi.get("biz_ext", {})
    avg_price = parse_int(biz_ext.get("cost"))      # 可能为None
    rating = parse_float(biz_ext.get("rating"))     # 可能为None
    opentime = biz_ext.get("opentime", "营业时间以高德地图为准")

    # 返回标准格式
    return {
        "name": poi["name"],
        "campus_area": campus_area,
        "cuisine": extract_type(poi.get("type")),
        "taste": [],                    # 高德无口味信息
        "avg_price": avg_price,
        "rating": rating,
        "distance": int(poi["distance"]),
        "opening_hours": opentime,
        "is_open": None,                # 高德无实时状态
        "allergens": [],
        "meal_time": ["午餐", "晚餐"],
        "latitude": latitude,
        "longitude": longitude,
        "address": poi.get("address", ""),
        "tel": poi.get("tel", ""),
        "source": "高德实时POI"
    }
```

## 四、API设计

### 4.1 接口列表

| 端点 | 方法 | 功能 | 参数 |
|------|------|------|------|
| /api/health | GET | 健康检查 | 无 |
| /api/foods | GET | 获取餐饮列表 | campus_area |
| /api/recommend | GET | 获取推荐 | 8个筛选参数 |

### 4.2 推荐接口详细设计

**请求**：
```
GET /api/recommend?campus_area=南门&max_price=30&min_rating=4.0
```

**参数**：
- `campus_area` (string, optional): 校区区域
- `keywords` (string, optional): 关键词搜索
- `taste` (string, optional): 口味，逗号分隔
- `max_price` (int, optional): 最高预算
- `max_distance` (int, optional): 最远距离（米）
- `min_rating` (float, optional): 最低评分
- `meal_time` (string, optional): 用餐时段
- `exclude_allergens` (string, optional): 过敏原，逗号分隔
- `limit` (int, default=10): 返回数量

**响应**：
```json
{
  "results": [
    { /* 餐厅对象 */ }
  ],
  "total_candidates": 20,      // 初始候选数
  "filtered_count": 5,         // 筛选后数量
  "returned_count": 5,         // 实际返回数量
  "using_realtime": false      // 是否使用实时数据
}
```

### 4.3 错误处理

**HTTP状态码**：
- 200: 成功
- 400: 参数错误
- 500: 服务器内部错误

**错误响应示例**：
```json
{
  "detail": "Invalid parameter: max_price must be positive"
}
```

## 五、前端架构

### 5.1 组件结构

```
App.vue (根组件)
├── Header (顶部标题和数据说明)
├── FilterPanel (左侧筛选面板)
│   ├── 校区区域选择
│   ├── 关键词搜索框
│   ├── 用餐时段选择
│   ├── 口味复选框组
│   ├── 预算输入框
│   ├── 距离输入框
│   ├── 评分选择
│   ├── 忌口复选框组
│   └── 操作按钮（获取推荐、重置）
├── ResultsSection (右侧结果区)
│   ├── ResultsHeader (结果统计)
│   ├── FoodGrid (餐厅卡片网格)
│   │   └── FoodCard × N (单个餐厅卡片)
│   └── MapSection (地图容器)
│       └── AMap (高德地图实例)
└── DetailDrawer (详情抽屉，条件渲染)
```

### 5.2 数据流

```
用户交互 → 修改filters → 调用getRecommendations()
                              ↓
                     发起HTTP请求到后端API
                              ↓
                    接收JSON响应并更新状态
                              ↓
       ┌────────────────────┬─────────────────┐
       ↓                    ↓                 ↓
  更新recommendations   更新统计信息     更新地图标记
       ↓                    ↓                 ↓
  重新渲染卡片网格      显示结果数量      添加Marker
```

### 5.3 状态管理

使用Vue 3 响应式数据：

```javascript
data() {
  return {
    filters: {             // 筛选条件
      campusArea: '',
      keywords: '',
      taste: [],
      maxPrice: null,
      maxDistance: null,
      minRating: null,
      mealTime: '',
      excludeAllergens: []
    },
    recommendations: [],   // 推荐结果
    filteredCount: 0,      // 筛选后数量
    usingRealtime: false,  // 数据源标识
    loading: false,        // 加载状态
    error: null,           // 错误信息
    selectedFood: null,    // 选中的餐厅
    map: null,             // 地图实例
    markers: [],           // 地图标记数组
    infoWindow: null       // 信息窗实例
  }
}
```

### 5.4 地图集成

**初始化**：
```javascript
initMap() {
  this.map = new AMap.Map('amap-container', {
    zoom: 15,
    center: [103.9348, 30.7606],  // 清水河校区
    mapStyle: 'amap://styles/normal'
  })
  this.map.addControl(new AMap.Scale())
  this.map.addControl(new AMap.ToolBar())
  this.infoWindow = new AMap.InfoWindow({
    offset: new AMap.Pixel(0, -30)
  })
}
```

**更新标记**：
```javascript
updateMapMarkers() {
  // 清除旧标记
  this.markers.forEach(m => m.setMap(null))
  this.markers = []

  // 添加新标记
  this.recommendations.forEach((food, index) => {
    if (!food.latitude || !food.longitude) return

    const marker = new AMap.Marker({
      position: [food.longitude, food.latitude],
      title: food.name,
      map: this.map
    })

    marker.on('click', () => {
      this.infoWindow.setContent(/* HTML */)
      this.infoWindow.open(this.map, marker.getPosition())
    })

    this.markers.push(marker)
  })

  // 自适应视野
  if (this.markers.length > 0) {
    this.map.setFitView(this.markers)
  }
}
```

## 六、安全性考虑

### 6.1 API Key管理

- **后端**：Web服务Key存储在 `.env` 文件，不暴露到前端
- **前端**：JS API Key写在HTML中，但已限制域名白名单
- **Git**：`.gitignore` 排除所有 `.env` 文件

### 6.2 CORS配置

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 生产环境应限制具体域名
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 6.3 输入验证

- FastAPI自动进行类型验证
- 前端使用 `v-model.number` 确保数字类型
- 后端对异常输入返回友好错误

## 七、性能优化

### 7.1 已实现的优化

1. **异步IO**：后端使用 `async/await` 避免阻塞
2. **条件渲染**：Vue使用 `v-if`/`v-show` 减少DOM操作
3. **事件防抖**：筛选条件变化不自动触发（可选开启）
4. **地图懒加载**：只在需要时创建标记

### 7.2 可优化项

1. **缓存**：对高德API响应增加Redis缓存
2. **CDN**：静态资源使用CDN加速
3. **分页**：大量结果时使用虚拟滚动
4. **SSR**：使用Nuxt.js实现服务端渲染

## 八、测试策略

### 8.1 单元测试（建议补充）

```python
# 测试推荐逻辑
def test_recommend_by_price():
    result = recommend_foods(max_price=20)
    assert all(f["avg_price"] <= 20 for f in result if f["avg_price"])

def test_recommend_by_allergens():
    result = recommend_foods(exclude_allergens=["花椒"])
    assert all("花椒" not in f["allergens"] for f in result)
```

### 8.2 集成测试（建议补充）

```python
# 测试API端点
async def test_health_check():
    response = await client.get("/api/health")
    assert response.status_code == 200
    assert response.json()["status"] == "ok"
```

### 8.3 端到端测试（建议补充）

使用Playwright或Cypress测试完整用户流程。

## 九、部署建议

### 9.1 开发环境

见《运行与测试指南.md》

### 9.2 生产环境

**后端**：
```bash
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker
```

**前端**：
```bash
npm run build
# 将 dist/ 部署到 Nginx 或 CDN
```

**反向代理（Nginx）**：
```nginx
server {
    listen 80;
    server_name example.com;

    location / {
        root /var/www/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://127.0.0.1:8000;
    }
}
```

## 十、版本演进

### v1.0（当前）
- 本地模拟数据
- 基础推荐算法
- 高德地图集成
- 双数据源支持

### v1.1（规划）
- 用户账号系统
- 收藏和历史记录
- 评价和反馈

### v2.0（规划）
- 数据库存储
- 实时更新推送
- 移动端适配优化
- 社交分享功能
